// Test stub for Next.js's `server-only` marker package. It exists only to make
// the import resolvable under the Node test environment; the real package is a
// build-time guard that has no runtime behavior. See vitest.config.ts alias.
export {};
