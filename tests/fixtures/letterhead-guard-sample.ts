/**
 * Not production code. This exists only so tests/letterhead-single-layout.ts
 * can pin its own comment stripper against every comment form that has
 * defeated it, rather than trusting that the stripper works.
 *
 * a block comment naming letterheadDesignLines()
 */

// whole line comment
export const KEEP = 'https://example.test/keep-me'; // trailing comment
