<?xml version="1.0" encoding="utf-8"?>
<!-- Advottic "Open cases" home-screen widget. Populated by
     CasesWidgetProvider from the SharedPreferences the WidgetBridge
     plugin writes. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="16dp">

    <!-- Header row: big open-case count + label -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="bottom">

        <TextView
            android:id="@+id/widget_open_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="—"
            android:textColor="#FBF7E9"
            android:textSize="34sp"
            android:textStyle="bold"
            android:includeFontPadding="false" />

        <TextView
            android:id="@+id/widget_open_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginBottom="5dp"
            android:text="Open cases"
            android:textColor="#D5BB7E"
            android:textSize="12sp"
            android:textAllCaps="true"
            android:letterSpacing="0.08" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="end|bottom"
            android:layout_marginBottom="5dp"
            android:text="Advottic"
            android:textColor="#5CFBF7E9"
            android:textSize="10sp"
            android:textAllCaps="true"
            android:letterSpacing="0.14" />
    </LinearLayout>

    <!-- Most recently updated case -->
    <TextView
        android:id="@+id/widget_latest"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text=""
        android:textColor="#FBF7E9"
        android:textSize="14sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Next hearing countdown -->
    <TextView
        android:id="@+id/widget_hearing"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="#B9F5EDDA"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Top recent action -->
    <TextView
        android:id="@+id/widget_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text=""
        android:textColor="#8FF5EDDA"
        android:textSize="11.5sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
