/**
 * Content script. Currently does nothing visible - the context-menu
 * entry from background.js fires regardless of any DOM injection.
 *
 * Future: a small in-page floating action button that picks up
 * highlighted text and posts to background. Skipping for v1 to keep
 * the bundle small and avoid CSP fights with apps like Gmail.
 */
