# Handover, 2026-08-15

35 commits from `6151739e` to `cabf1843`. Suite 3800 to 3981, all green,
build and audit guards clean, working tree clean.

Written because Wednesday's meeting is close and several items below need a
decision or a device rather than more code.

---

## Needs you, and nothing moves without it

**Two commits that email real people, still held.**
`fa21d75` (deadline reminder sweep) and `eaa4026d` (a daily sweep telling the
two waiting parties). Both are off `main`. They send mail to actual users on a
cron. Say go and they merge.

**Item 5, the rotating signing code.** Designed in
`docs/superpowers/specs/2026-08-15-rotating-signing-code-design.md`. The
implementation is straightforward; the check that decides it is that a real
phone camera still reads the rotating code, which no test here can give.

**Item 7, signature anchors.** Diagnosed in
`docs/superpowers/specs/2026-08-15-signature-anchor-text-extraction.md`, with
real coordinates extracted from the NDA you sent. Needs the placed fields
rendered and page 8 looked at.

**GTM phases 2 to 4.** Blocked on budget, team, target accounts and baselines.

**Six stale branches.** `fix/copy-truth`, `fix/customizer-i18n`,
`feat/api-token-revoke`, `fix/case-section-icons`,
`fix/unchecked-supabase-writes`, `fix/firm-pool-grant-callsite`. All nine of
their commits are now delivered by content. Safe to delete; left alone because
that is your call.

---

## Verify on the next production deploy

**The signing surface should lose its marketing chrome.** `edd8e992` adds
`/sign` to the same gate that already strips `/safe`. It cannot be proved
locally: the gate reads `x-pathname`, which middleware sets, and under
`next dev` that header is absent so every route gate fails open. Re-run this
after deploy and expect `/sign` to join `/safe` at "no":

```
production  /                 header: yes
production  /safe             header: NO
production  /sign/<token>     header: yes   <- should become NO
```

**The schema drift gate should go green.** `50a326f3` regenerated
`supabase/schema-fingerprint.sha256` (`4caf11a4` to `004c04fc`) from the live
schema, computed server-side and length-checked.

---

## What shipped

**Signing.** The desk now notices the phone in ~1.2s instead of up to 8
(`7db98c02`). Phone is a fourth tab beside Draw, Type and Upload rather than a
card below them (`0d0c9c7c`). A signed phone no longer offers its pad on a back
press (`b2572c65`). The intent affirmation is enforced on the SERVER, not only
in the browser: `/api/firm/mark` accepted a mark with the field absent, storing
null and landing the signature anyway (`60a0afb6`).

**The signature line now carries date, time and zone**, pinned to UTC so the
browser and the server cannot disagree on one instrument (`462de3d5`). Four
separate formatters existed; they are one (`a689882d`). A note claiming a
provenance gap that did not exist is corrected with the full caller trace
(`205e5db3`).

**Safe Witness discreet mode** conceals rather than only covering: the page
behind the black overlay was still in the tab order and the accessibility tree
(`a162afc7`).

**Money.** The firm token pool had no production caller at all, so multi-seat
firms were never credited what their plan sells. Cherry-picked forward rather
than merged, because `main` already had a stronger version of a sibling commit
that a merge would have reverted.

**Security.** A cross-tenant escalation on `api_tokens` closed and the table
written down (`4542b462`). Deleting a firm's written policy now asks first and
names the policy (`cabf1843`).

**Reports.** Figures grouped into four named views, with a guard that every
figure lands in exactly one (`9cb2b98d`).

---

## The thread running through the day

Recorded in `docs/ENGINEERING_LEARNINGS.md`, because it cost real time four
separate times:

**Check the target, and check main, before believing anything.**

- The contrast audit was pointed at `next dev`, whose stylesheet lacks the
  repaint layer, and reported **372 AA failures**. Production the same hour:
  **0 of 3083**. About 250 call-site edits were written for a defect that did
  not exist, and thrown away. The audit now refuses to measure a build without
  the layer.
- A branch would have **reverted a better fix** already on `main`.
- QR single-use was about to be **rebuilt on top of itself**.
- Six branches reported 5, 3, 3, 1, 1, 1 outstanding commits. Checked by
  content: **eight of nine already delivered**, one byte-identical.

`git rev-list --count` measures SHA reachability. It cannot tell you whether
the work is delivered. Only applying the patch can.

Three of my own guards were green and proving nothing until mutation caught
them: one matched a `pushState` inside the very handler it was meant to hold,
one banned a word its own explanatory comment had to use, and one measured an
`.update()` in a different function entirely. Each is documented where it
lives.
